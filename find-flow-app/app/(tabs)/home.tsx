import AppSafeAreaView from '@/components/AppSafeAreaView';
import { Text } from 'react-native';

const Home = () => {
  return (
    <AppSafeAreaView>
      <Text>Home</Text>
    </AppSafeAreaView>
  );
};

export default Home;
